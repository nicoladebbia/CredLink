{
  "name": "@credlink/sw-relay",
  "version": "0.1.0",
  "description": "Service Worker relay for C2PA manifests (offline + stale-while-revalidate)",
  "type": "module",
  "main": "dist/sw.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
