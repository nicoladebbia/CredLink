{
  "name": "@credlink/edge-worker",
  "version": "0.1.0",
  "description": "Cloudflare Edge Worker for C2 Concierge",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev --local --port 8787",
    "deploy": "wrangler deploy",
    "build": "tsc"
  },
  "dependencies": {
    "@credlink/policy": "workspace:*",
    "@credlink/utils": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231025.0",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.22.0"
  }
}
