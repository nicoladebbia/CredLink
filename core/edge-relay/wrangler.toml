name = "c2-edge-relay"
main = "src/index.ts"
compatibility_date = "2024-01-10"

[vars]
# TENANT_CONFIG should be JSON mapping tenant -> configuration.
# Example:
# {
#   "tenant-a": {
#     "secret": "base64-hmac-secret",
#     "allowlist": ["manifests.tenant-a.com", "*.trusted.tenant-a.com"],
#     "originSigningSecret": "base64-origin-secret"
#   }
# }
TENANT_CONFIG = "{}"
