{
  "name": "@credlink/edge-relay",
  "version": "0.1.0",
  "description": "Cloudflare Edge Relay for zero-CORS manifest delivery",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev --local --port 8788",
    "deploy": "wrangler deploy",
    "build": "tsc"
  },
  "dependencies": {
    "@credlink/utils": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241018.0",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.84.0"
  }
}
