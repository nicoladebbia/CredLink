# CredLink pnpm Configuration
# Fix module resolution issues in monorepo

# Enable shamefully-hoist to lift dependencies to root level
shamefully-hoist=true

# Public hoist patterns for commonly used packages
public-hoist-pattern[]=*types*
public-hoist-pattern[]=express*
public-hoist-pattern[]=redis*
public-hoist-pattern[]=ts-node*
public-hoist-pattern[]=nodemon*

# Prefer deduplication to reduce node_modules complexity
prefer-frozen-lockfile=true
strict-peer-dependencies=false
